#include <iostream>
#include <math.h>
#include <algorithm>
using namespace std;

#define MAX_DEGREE 101

typedef struct { 
	int coef;
    int expon; // 지수
} polynomial;

    polynomial A[MAX_DEGREE] = {{8,3},{7,1},{1,0}};
    polynomial B[MAX_DEGREE] = {{10,4},{3,2},{1,0}};

int main() {
    

    return 0;
}